## コンポーネントを純粋に保つ

https://ja.react.dev/learn/keeping-components-pure

関数型プログラミング的な考えの話かな。  

> 純関数とは計算だけを行い、他には何もしない関数のことです。

### このページで学ぶこと

- 「純粋」であるとは何か、それによりなぜバグが減らせるのか
- 変更をレンダーの外で行い、コンポーネントを純粋に保つ方法
- Strict Mode を使用してコンポーネントの間違いを見つける方法

このあたりを学んで行く。  

### 純粋性：コンポーネントとは数式のようなもの

- 自分の仕事に集中する。呼び出される前に存在していたオブジェクトや変数を変更しない。
- 同じ入力には同じ出力。同じ入力を与えると、純関数は常に同じ結果を返す。

副作用がないとか参照透過性とか…。

> React は、あなたが書くすべてのコンポーネントが純関数であると仮定しています。

なるほど。  
純関数でない場合はコンポーネントとして問題があるってことかな。  


```js

export default function App() {
  return (
    <section>
      <h1>Spiced Chai Recipe</h1>
      <h2>For two</h2>
      <Recipe drinkers={2} />
      <h2>For a gathering</h2>
      <Recipe drinkers={4} />
    </section>
  );
}

```

こんな感じで呼び出しているが常に同じJSXが返されるよという例。  

ちなみに、{"2"}で呼び出すのと"2"で呼び出すのは意味的に同じであることをちょっと調べて分かった。  
文字列リテラルでもOKということ。  

`{2}` はJavaScriptの構文を使えるように`{}`で括った後に数値リテラルである2を渡しているということになる。  

### 副作用：意図せぬ (?) 付随処理

```js
let guest = 0;

function Cup() {
  // Bad: changing a preexisting variable!
  guest = guest + 1;
  return <h2>Tea cup for guest #{guest}</h2>;
}

export default function TeaSet() {
  return (
    <>
      <Cup />
      <Cup />
      <Cup />
    </>
  );
}
```

この例だと関数の外にある変数`guest`が関数呼び出しの際にインクリメントされているので、呼び出しごとに値が変わってしまう…！  

> このコンポーネントは、外部で宣言された guest 変数を読み書きしています。つまり、このコンポーネントを複数回呼び出すと、異なる JSX が生成されます！ さらに悪いことに、ほかのコンポーネントも guest を読み取る場合、それらもレンダーされたタイミングによって異なる JSX を生成することになります！ これでは予測不可能です。

あー確かに、この例だと3回コンポーネント使われているのでどのタイミングで値が変わるのかわからないですね…。  

で修正は以下のようになると。  

```js
function Cup({ guest }) {
  return <h2>Tea cup for guest #{guest}</h2>;
}

export default function TeaSet() {
  return (
    <>
      <Cup guest={1} />
      <Cup guest={2} />
      <Cup guest={3} />
    </>
  );
}
```

`props`として渡せばよいと。  

ここで疑問に思ったのでClaudeに質問してみた。

```
guest={1} として渡している1は関数の引数として渡していると表現するのが適切？それと関数のpropsとして渡しているが適切？どちらも意図するものは同じだし、どっちもあっているとおもっているのだけど
```

回答

```
どちらも正しいです。ただ使い分けのニュアンスがあります。

「propsとして渡す」 → Reactの文脈での表現

JSXで <Cup guest={1} /> と書くとき
Reactコンポーネントの話をしているとき
一般的にReactの会話ではこちらをよく使う

「引数として渡す」 → JavaScriptの関数としての表現

Cup は結局ただの関数なので技術的には正確
ただし厳密には guest={1} 単体が引数ではなく、{ guest: 1 } というオブジェクトにまとめられてから引数として渡される

なので厳
密さで言うと：
js// 実際にJSの関数として見るとこうなっている
Cup({ guest: 1 })
// guest={1} だけが引数なのではなく、propsオブジェクト全体が引数
結論としては、Reactの話をしている文脈では「propsとして渡す」の方がより自然で正確なニュアンスが出ます。「引数として渡す」も間違いではないですが、「propsオブジェクトの一部として渡す」が本当に正確な表現ですね。
```

というわけ。  

結構前であった分割代入の話を忘れていたので、`{ guest : 1 }` として渡していることを失念していたので改めて覚えておく。  

### StrictMode で純粋でない計算を検出

お、いいね～。  

> まだ全部を使ったことはないかもしれませんが、React には props、state、そしてコンテクストという、レンダー中に読み取ることができる 3 種類の入力値があります。これらの入力値は、常に読み取り専用として扱うようにしてください。

なるほど。  

> 要素のレンダー中に既存の変数やオブジェクトを書き換えることは絶対にやってはいけません。

ハイ・・。  


> React には “Strict Mode” という機能があり、開発中には各コンポーネント関数を 2 回呼び出します。関数呼び出しを 2 回行うことで、Strict Mode はこれらのルールに反するコンポーネントを見つけるのに役立ちます。

あ～なるほど。  

このチュートリアルでconsole.logが2回呼び出されているのに違和感があったけどStrictModeだったからか。  

> 元の例では “Guest #1”、“Guest #2”、“Guest #3” と表示される代わりに “Guest #2”、“Guest #4”、“Guest #6” と表示されてしまっていましたね。元の関数が純粋でなかったため、2 回呼び出すと壊れていたわけです。

これもStrictModeだからこうなっていたのか。  

> Strict Mode は本番環境では影響を与えないため、ユーザが使うアプリを遅くすることはありません。Strict Mode を有効にするには、ルートコンポーネントを <React.StrictMode> でラップします。一部のフレームワークでは、これがデフォルトで行われます。

単純だけどよくできた仕組みだ…。  

### ローカルミューテーション：コンポーネントの小さな秘密

> レンダー中にその場で作成した変数やオブジェクトであれば、書き換えることは全く問題ありません。

関数内に宣言したものであれば問題ないということ。  

> これは “ローカルミューテーション (local mutation)”

名前付いていたんだ。  

### 副作用を引き起こせる場所

> 関数型プログラミングには純粋性が重要であるとはいえ、いつか、どこかの場所で、何らかのものが変化しなければなりません。むしろそれがプログラミングをする意味というものでしょう。

確かに  

> これらの変化（スクリーンの更新、アニメーションの開始、データの変更など）は 副作用 (side effect) と呼ばれます。

はい。  

> React では、副作用は通常、イベントハンドラの中に属します。イベントハンドラは、ボタンがクリックされたといった何らかのアクションが実行されたときに React が実行する関数です。

ふむ。  

> イベントハンドラは、コンポーネントの「内側」で定義されているものではありますが、レンダーの「最中」に実行されるわけではありません！ つまり、イベントハンドラは純粋である必要はありません。

レンダーの最中でなければ純粋である必要はないと。  
レンダーの部分は必ず純粋でないと描画タイミングでおかしくなるからということかな。  

> いろいろ探してもあなたの副作用を書くのに適切なイベントハンドラがどうしても見つからない場合は、コンポーネントから返された JSX に useEffect 呼び出しを付加することで副作用を付随させることも可能です。

出たな`useEffect`。  
Reactほとんど知らないけど度々議論されているのを見るぞ。  

> これにより React に、その関数をレンダーの後（その時点なら副作用が許されます）で呼ぶように指示できます。ただしこれは最終手段であるべきです。

まあそういう立ち位置だよね。  
Hack的なものだよね。  

> 可能な限り、ロジックをレンダーのみで表現してみてください。これだけでどれだけのことができるのか、驚くことでしょう！

了解です。  


### React はなぜ純粋性を重視するのか？

- コンポーネントが異なる環境、例えばサーバ上でも実行できるようになります！ 入力値が同じなら同じ結果を返すので、ひとつのコンポーネントが多数のユーザリクエストを処理できます。

おーなるほど。サーバでも実行できるっていうのは聞きましたよ！  

- 入力値が変化しない場合、レンダーをスキップすることでパフォーマンスを向上できます。これが問題ないのは、純関数は常に同じ出力を返すため安全にキャッシュできるからです。

キャッシュかー。なるほどね。  
レンダーのキャッシュができるからね。  

- 深いコンポーネントツリーのレンダーの途中でデータが変化した場合、React は既に古くなったレンダー処理を最後まで終わらせるような無駄を省き、新しいレンダーを開始できます。純粋性のおかげで、いつ計算を中断しても問題ありません。

おーこれも確かにそうだ。  

### まとめ


- コンポーネントは純粋である必要がある。すなわち：
  - コンポーネントは自分の仕事に集中する。レンダー前に存在していたオブジェクトや変数を書き換えない。
  - 入力が同じなら出力も同じ。同じ入力に対しては、常に同じ JSX を返すようにする。
- レンダーはいつでも起こる可能性があるため、コンポーネントは相互の呼び出し順に依存してはいけない。
- コンポーネントがレンダーに使用する入力値を書き換えない。これには props、state、コンテクストが含まれる。画面を更新するためには既存のオブジェクトを書き換えるのではなく、代わりに state をセットする。
- コンポーネントのロジックはできるだけコンポーネントが返す JSX の中で表現する。何かを「変える」必要がある場合、通常はイベントハンドラで行う。最終手段として useEffect を使用する。
- 純関数を書くことには訓練が必要だが、それにより React パラダイムの威力が発揮される。


### チャレンジ問題にトライ


#### 1. 壊れた時計を修理

```js
import { useState, useEffect } from 'react';
import Clock from './Clock.js';

function useTime() {
  const [time, setTime] = useState(() => new Date());
  useEffect(() => {
    const id = setInterval(() => {
      setTime(new Date());
    }, 1000);
    return () => clearInterval(id);
  }, []);
  return time;
}

export default function App() {
  const time = useTime();
  return (
    <Clock time={time} />
  );
}
```

こんな感じで書いてみた。  

模範解答と大体同じなのでOK  

#### 2. 壊れたプロフィールを修正する


```js
import Panel from './Panel.js';
import { getImageUrl } from './utils.js';

export default function Profile({ person }) {
  return (
    <Panel>
      <Header person={person} />
      <Avatar person={person} />
    </Panel>
  )
}

function Header({ person }) {
  return <h1>{person.name}</h1>;
}

function Avatar({person}) {
  return (
    <img
      className="avatar"
      src={getImageUrl(person)}
      alt={person.name}
      width={50}
      height={50}
    />
  );
}
```

`person`をHeader, Avatarコンポーネントのpropsとして追加して終了。  

### 壊れたストーリートレイを修正

```js
export default function StoryTray({ stories }) {
  let localStories = stories.map(story=>story);
  localStories.push({
    id: 'create',
    label: 'Create Story'
  });

  return (
    <ul>
      {localStories.map(story => (
        <li key={story.id}>
          {story.label}
        </li>
      ))}
    </ul>
  );
}
```

雑にコピーしてローカルミューテーション！  
でいいような。  

模範解答を見ると↑は別解みたいな形だった。  

そもそも、HTMLにJSXとしてCreate Storyを一番下に追加するが良さそう。  
で、配列はそのまま表示。  
実務ならこれですね。  







